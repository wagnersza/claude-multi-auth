{
  "name": "@wsza/claude-multi-auth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Run multiple Claude Code instances with different auth methods from a single session.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^1.0.0"
  }
}
